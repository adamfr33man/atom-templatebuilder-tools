'use babel';

module.exports.lintText = (text, path) => {

  const messages = [],
      minitemplate = {
        minitemplates: {}
      };

      // messages.push({
      //   type: 'Error',
      //   text: 'Test Error',
      //   range:[ [0, 0], [0, 1] ],
      //   filePath: path
      // });

  return messages;
};
